<table> PuzzleLoader {
    <void> function PuzzleLoader.new(<table> {
        <table> Grid,
        <table> Solution,
    })
    -- [!] This function is only compatible with Jailbreak's puzzle outputs
}
  
return <table> PuzzleLoader
